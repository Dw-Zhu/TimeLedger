<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.finance.mapper.BillMapper">

    <resultMap id="BillVoResultMap" type="com.finance.vo.BillVo">
        <id property="id" column="id" />
        <result property="type" column="type" />
        <result property="amount" column="amount" />
        <result property="remark" column="remark" />
        <result property="billDate" column="bill_date" />
        <result property="accountName" column="account_name" />
        <result property="categoryName" column="category_name" />
    </resultMap>

    <select id="selectUserBillDetails" resultMap="BillVoResultMap">
        SELECT
        b.id,
        b.type,
        b.amount,
        b.remark,
        b.bill_date,
        a.name AS account_name,
        c.name AS category_name
        FROM finance_bill b
        LEFT JOIN finance_account a ON b.account_id = a.id
        LEFT JOIN finance_category c ON b.category_id = c.id
        <where>
            <if test="userId != null">
                b.user_id = #{userId}
            </if>
        </where>
        ORDER BY b.bill_date DESC
    </select>

</mapper>