<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.finance.mapper.DiaryReminderMapper">

    <resultMap id="DiaryReminderResultMap" type="com.finance.entity.DiaryReminder">
        <id property="id" column="id" />
        <result property="userId" column="user_id" />
        <result property="diaryContent" column="diary_content" />
        <result property="moodStatus" column="mood_status" />
        <result property="reminderTitle" column="reminder_title" />
        <result property="reminderTime" column="reminder_time" />
        <result property="reminderThreshold" column="reminder_threshold" />
        <result property="isReminded" column="is_reminded" />
        <result property="recordDate" column="record_date" />
        <result property="createTime" column="create_time" />
        <result property="updateTime" column="update_time" />
    </resultMap>

    <select id="selectByRecordDate" resultMap="DiaryReminderResultMap">
        SELECT id, user_id, diary_content, mood_status, reminder_title,
        reminder_time, reminder_threshold, is_reminded, record_date, create_time, update_time
        FROM user_diary_reminder
        WHERE user_id = #{userId} AND record_date = #{recordDate}
    </select>

    <insert id="insertDiaryReminder" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_diary_reminder (
        user_id, diary_content, mood_status, reminder_title,
        reminder_time, reminder_threshold, is_reminded, record_date, create_time, update_time
        ) VALUES (
        #{userId}, #{diaryContent}, #{moodStatus}, #{reminderTitle},
        #{reminderTime}, #{reminderThreshold}, #{isReminded}, #{recordDate}, #{createTime}, #{updateTime}
        )
    </insert>

    <update id="updateDiaryReminder">
        UPDATE user_diary_reminder
        <set>
            <if test="diaryContent != null">diary_content = #{diaryContent},</if>
            <if test="moodStatus != null">mood_status = #{moodStatus},</if>
            <if test="reminderTitle != null">reminder_title = #{reminderTitle},</if>
            <if test="reminderTime != null">reminder_time = #{reminderTime},</if>
            <if test="reminderThreshold != null">reminder_threshold = #{reminderThreshold},</if>
            <if test="isReminded != null">is_reminded = #{isReminded},</if>
            <if test="updateTime != null">update_time = #{updateTime}</if>
        </set>
        WHERE id = #{id} AND user_id = #{userId}
    </update>

</mapper>